name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 28.1 is the floor this package and magit-section both declare.
        emacs: ['28.1', '30.1']
    name: emacs ${{ matrix.emacs }}
    steps:
      - uses: actions/checkout@v4

      - uses: purcell/setup-emacs@master
        with:
          version: ${{ matrix.emacs }}

      # ghostel is on no archive, and nothing here needs it to compile:
      # herdr-term.el declares its functions and requires it at runtime.
      - name: Install magit-section
        run: |
          emacs -Q --batch --eval "(progn \
            (require 'package) \
            (add-to-list 'package-archives '(\"nongnu\" . \"https://elpa.nongnu.org/nongnu/\") t) \
            (add-to-list 'package-archives '(\"melpa\" . \"https://melpa.org/packages/\") t) \
            (package-initialize) \
            (package-refresh-contents) \
            (package-install 'magit-section))"

      - run: make compile
      - run: make lint
      - run: make test
